# 2.6.x

작업 목표 1 (모듈 분리): `caption.js` Interface(UI 셸) 도메인을 `modules/ui/`로 분리.

---

## 2.6.1 — 2026-09-06

### Changed

- `ui.initialize` 주입 API 정리
  - `getSheet` 제거 → `sheet` 직접 주입
  - `getActionContext` 제거 → `video` · `Fn` · `Shortkey` · `import`/`export` getter를 `initialize`에 평평하게 전달
  - `createWidgets(context)` — deps 객체 래핑 제거
- `ui.initialize` 호출을 `initializeDomainModules()` 안으로 이동 (ready 시 sheet·video·subtitle과 함께 조립)

| 파일 | 내용 |
|------|------|
| `VERSION` | `2.6.1` |
| `index.html` | About `2.6.1` |
| `CHANGELOG.md` | `2.6.1` 항목 |
| `public/js/caption.js` | `initializeDomainModules`에서 `ui.initialize` |
| `public/js/modules/ui/index.js` | `initialize(context)` · `createWidgets(context)` |
| `public/js/modules/ui/dialog.js` | `sheet` 직접 사용 |
| `public/js/modules/ui/widgets.js` | `createWidgets(context)` |

---

## 2.6.0 — 2026-09-06

### Changed

- `caption.js` Interface · Toast를 `public/js/modules/ui/`로 분리
  - `uiModule()` · `initialize({ i18n, getSheet, getActionContext })`로 API 주입
  - 공개 식별자 `Interface` → `ui` (sheet · video · subtitle · caption)
- UI API camelCase 통일 · ready 시 `ui.init()` 1회 바인딩
  - DOM: `wrap` · `layout` · `layer` · `anotherInput`
  - feedback: `alert` · `success` · `confirm`
  - dialog(공개): `dialog.open` / `dialog.close` · `switchFocus`
  - select(공개): `ui.select({ key, value })`
  - confirm 옵션: `successLabel` / `cancelLabel` (버튼 문구)
  - `applyI18n`
  - `tab` · `inputFile` · `dialog.init` · `select.init`은 `ui.init()` 내부 전용
- ui 모듈 jQuery 제거 (바닐라 DOM · `bindEvent`)
  - `toast.js` — 단일 `show({ type, message, title, options })`
  - `feedback.js` — alert / success / confirm
  - `dialog.js` — dialog · switchFocus
  - `widgets.js` — tab · select · inputFile · toggle
  - `applyI18n.js` — DOM i18n · select trigger 문구 동기화
- `wrap` / `layout`을 `querySelector`로 보관 (caption Shortkey·nav도 네이티브 호출)

### Fixed

- confirm `content`에 i18n `<br />`가 보이도록 `innerHTML` 적용 (title은 `textContent` 유지)

### Docs

- README: 레거시 구조(기준선)와 현재 구조를 분리, 작업 목표 상태·scope를 모듈에 맞춤
- changelog 2.1–2.6 헤더:「README 1단계」→「작업 목표 1 (모듈 분리)」

### Note

- `Shortkey` · `Fn.Format` / `Fn.Language`는 caption.js에 유지 (후속 분리)
- caption 잔여 jQuery(단축키·ready 일부)는 후속 작업

| 파일 | 내용 |
|------|------|
| `VERSION` | `2.6.0` |
| `index.html` | About `2.6.0` |
| `CHANGELOG.md` | `2.6.0` 항목 |
| `README.md` | 기준선 · 현재 구조 · scope |
| `public/js/modules/ui/index.js` | `uiModule` · `initialize` · `init` |
| `public/js/modules/ui/toast.js` | `createToast` |
| `public/js/modules/ui/feedback.js` | `createFeedback` |
| `public/js/modules/ui/dialog.js` | `createDialog` |
| `public/js/modules/ui/widgets.js` | `createWidgets` |
| `public/js/modules/ui/applyI18n.js` | `createApplyI18n` |
| `public/js/caption.js` | Interface·Toast 인라인 제거, `ui.initialize` · `ui.init` |
| video · subtitle · sheet | `ui.*` 참조 갱신 |
