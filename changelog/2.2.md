# 2.2.x

README 1단계: `caption.js` 범용 유틸을 `modules/utils/`로 분리.

---

## 2.2.0 — 2026-08-31

### Changed

- `caption.js` 하단 유틸·프로토타입 확장을 `public/js/modules/utils/utils.js`로 분리
- zero-pad: `.zf()` 프로토타입 → `padZero()` (`String.padStart`)
- IE용 `Array.find` / `findIndex` 폴리필 제거 (IE 지원 종료 이후 불필요)
- 미사용 `Date.prototype.format` 제거

| 파일 | 내용 |
|------|------|
| `VERSION` | `2.2.0` |
| `index.html` | About `2.2.0` |
| `public/js/modules/utils/utils.js` | `clone`, `capitalize`, `extend`, `padZero` |
| `public/js/caption.js` | utils import, 인라인 유틸·폴리필 제거 |

### Note

- `Toast`는 UI(jQuery 의존)라 utils에 포함하지 않음 — 추후 UI 모듈 분리

---

## 2.2.1 — 2026-08-31

### Changed

- `Fn.Data` / `Fn.Log` / `Fn.Hour` / `Fn.Second`를 `modules/utils/`로 분리하고 API 전환
  - `storage.get` / `set` / `remove`
  - `editHistory` — `index`, `entries`, `prev`, `next`, `push`, `clear` (`utilsModule()`)
  - `formatTimecode`, `parseTimecode`, `splitTimecode`, `timePartsToMs`
- `Sheet.Edit.TimeControl` 시간 계산을 `time.js`로 통합
- `utils.js` → `object.js` / `string.js` 분리, `index.js`에서 `utilsModule()`로 통합 초기화

| 파일 | 내용 |
|------|------|
| `VERSION` | `2.2.1` |
| `index.html` | About `2.2.1` |
| `public/js/modules/utils/index.js` | `utilsModule()` — storage, editHistory, 유틸 함수 |
| `public/js/modules/utils/object.js` | `clone`, `extend` |
| `public/js/modules/utils/string.js` | `capitalize`, `padZero` |
| `public/js/modules/utils/storage.js` | `storage` |
| `public/js/modules/utils/editHistory.js` | `editHistory` |
| `public/js/modules/utils/time.js` | 타임코드 유틸 |
| `public/js/caption.js` | `utilsModule()` import·호출부 전환 |

### Note

- `Fn.Format`, `Fn.Language`, `Fn.Encode*` 등은 utils가 아닌 **앱 설정·자막 포맷·인코딩 옵션** 영역 — `caption.js`에 유지, 추후 도메인별 모듈 분리

---

## 2.2.2 — 진행 중 (`refactor/remove-eval`)

`caption.js` eval 제거.

### 2.2.2-dev.1 — 2026-08-31

#### Changed

- `Subtitle.Encode` 색상 eval → `normalizeElementColor` (`public/js/modules/utils/color.js`)
  - `cssColorToHex` — `rgb()` / `#hex` → `#RRGGBB`
  - detached DOM: `color` attr · `style` 문자열 파싱 (렌더/append 없음)

#### Fixed

- `Interface.Confirm` `ev.close` for-in 변수 `let` 선언 — ES module strict mode에서 확인 다이얼로그가 닫히지 않던 문제

| 파일 | 내용 |
|------|------|
| `public/js/modules/utils/color.js` | `cssColorToHex`, `normalizeElementColor` |
| `public/js/modules/utils/index.js` | color 유틸 export |
| `public/js/caption.js` | `Subtitle.Encode` eval 제거, Confirm fix |

#### Docs

- README — dev 커밋 버전(`X.Y.Z-dev.N`) 정책, fix/docs는 changelog 관리
- changelog `2.2.2-dev.1` 기록

| 파일 | 내용 |
|------|------|
| `README.md` | dev 커밋 버전·커밋 메시지/changelog 역할 |
| `changelog/2.2.md` | `2.2.2-dev.1` |

#### Note

- `VERSION` · About · tag는 **2.2.2 완료 시** 반영 (현재 `2.2.1`)

### 2.2.2-dev.2 — 2026-08-31 (진행 중)

#### Changed

- `data-callback` → `data-action` 통일 (Tab · Select · File input)
- `data-action` eval 제거 → `runAction` 경로 해석 (`resolveAction`)
- Import/Export/Shortkey tab `data-action`에서 `()` 제거 (eval·registry 공통 호환)
- 미구현·미사용 `Subtitle.FileCheck` `data-action` 제거 (import 확장자 검증은 Load 버튼에서 처리)

#### Fixed

- `Sheet.DrawHTML.srt` — `drawTimelineInfo` 인자 누락 (ES module strict mode에서 SRT draw `ReferenceError`)
- `Sheet.Set` — `o.format` → `o.Format` 오타 (포맷 전환 col 보정 조건)
- 일본어 `dur` 컬럼 헤더 `デュレーション` → `長さ` (80px 컬럼 넘침)

| 파일 | 내용 |
|------|------|
| `index.html` | `data-action` 전환, tab `()` 제거, SRT file input action 제거 |
| `public/js/caption.js` | SRT draw `drawTimelineInfo`, Sheet.Set 포맷 조건, `data-action` · eval 제거 |
| `public/js/modules/utils/action.js` | `resolveAction` · `runAction` |
| `public/js/modules/i18n/jp.js` | `dur-smi` · `dur-srt` 헤더 단축 |

#### Note

- `data-action` eval → `runAction` 경로 해석으로 전환 (`modules/utils/action.js`)
